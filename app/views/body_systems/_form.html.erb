<%= form_for(@body_system) do |f| %>
  <% if @body_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@body_system.errors.count, "error") %> prohibited this body_system from being saved:</h2>

      <ul>
      <% @body_system.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :order %><br />
    <%= f.number_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
